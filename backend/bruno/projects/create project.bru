meta {
  name: create project
  type: http
  seq: 2
}

post {
  url: http://localhost:5000/api/projects
  body: json
  auth: none 
}

body:json {
  {
    "title": "New Project from Bruno",
    "description": "This is a test project created from the Bruno client."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
