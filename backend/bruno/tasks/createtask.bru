meta {
  name: create task
  type: http
  seq: 1
}

post {
  url: http://localhost:5000/api/projects/:projectId/tasks
  body: file create task.bru.json
  auth: inherit
}

params:path {
  projectId: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
